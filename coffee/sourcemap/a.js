// Generated by CoffeeScript 1.6.2
(function() {
  var Animal, Horse, Snake, animal, button, horse, name, snake, _i, _len, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Animal = (function() {
    function Animal(name) {
      this.name = name;
      this.type = 'Animal';
    }

    Animal.prototype.run = function(speed) {
      var _ref;

      if (speed == null) {
        speed = 0;
      }
      return alert("I'm " + this.name + ", i'm " + ((_ref = this.type.substr(0, 1).toLowerCase()) === 'a' || _ref === 'e' || _ref === 'i' || _ref === 'o' || _ref === 'u' ? 'an' : 'a') + " " + this.type + ", and i'm running(very fast~~).");
    };

    return Animal;

  })();

  Snake = (function(_super) {
    __extends(Snake, _super);

    /*
    	arguments:
    		name String
    		length Number
    		age Number
    */


    function Snake(options) {
      this.name = options.name, this.length = options.length, this.age = options.age;
      Snake.__super__.constructor.call(this, this.name);
      this.type = 'Snake';
    }

    Snake.prototype.run = function(speed) {
      if (speed == null) {
        speed = 5;
      }
      Snake.__super__.run.call(this, speed);
      return alert("my track are beatiful.				my speed is " + speed + ".");
    };

    return Snake;

  })(Animal);

  Horse = (function(_super) {
    __extends(Horse, _super);

    function Horse(options) {
      this.name = options.name, this.height = options.height, this.age = options.age;
      Horse.__super__.constructor.call(this, this.name);
      this.type = 'Horse';
    }

    Horse.prototype.run = function(speed) {
      if (speed == null) {
        speed = 45;
      }
      Horse.__super__.run.call(this, speed);
      return alert("i'm more fase than Snake,					my speed is " + speed);
    };

    return Horse;

  })(Animal);

  animal = new Animal("original D animal");

  snake = new Snake({
    name: "pretty snake",
    length: "180cm",
    age: 3
  });

  horse = new Horse({
    name: "strong horse",
    height: "200cm",
    age: 6
  });

  button = {};

  _ref = ['animal', 'snake', 'horse'];
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    name = _ref[_i];
    button["" + name] = document.querySelector("#" + name + ">.run");
  }

  window.animalButtons = button;

  document.addEventListener('DOMContentLoaded', function() {
    return document.querySelector('body').addEventListener('click', function(e) {
      var parentDom, type;

      parentDom = e.target.parentNode;
      type = parentDom.getAttribute('id');
      if (type === 'animal') {
        animal.run();
      }
      if (type === 'snake') {
        snake.run();
      }
      if (type === 'horse') {
        return horse.run(58);
      }
    });
  });

}).call(this);

/*
//@ sourceMappingURL=a.map
*/
