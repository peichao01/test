!!! 5
html
	head
		title #{title}
	body
		h1 登录页面

		form(id="login-form", action=loginShowPath(), method="POST")
			| 邮箱：
			input(type="text", name="email")
			br

			| 密码：
			input(type="password", name="password")
			br

			input(type="submit", value="登录")
			br

			a(href=registerShowPath()) 立即注册